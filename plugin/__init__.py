"""Plugin World Model POC — isolated macOS perception/runtime subsystem.

Hermes (Layer 0) stays unchanged. This package owns observation → entity
fusion → world model → Ghost execution. Soft-delete the tree if the
hypothesis fails.
"""

from __future__ import annotations

__version__ = "0.2.0"

__all__ = ["__version__"]
